SQL queries:
1. CREATE TABLE CUSTOMER(CUSTOMER_ID INT, 
			 NAME VARCHAR2(30), 
			 PHONE INT, 
			 EMAIL VARCHAR2(30), 
			 ADD VARCHAR2(40));

2. CREATE TABLE MENU(ITEM_ID INT, 
		     ITEM_NAME VARCHAR2(30), 
		     CATEGORY VARCHAR(10), 
		     PRICE FLOAT, 
		     AVAILABILITY VARCHAR2(15));

3. CREATE TABLE ORDERS(ORDER_ID INT, 
		       CUSTOMER_ID INT, 
		       TOTAL_AMOUNT FLOAT, 
		       STATUS VARCHAR2(20));
